export type ElementPosition = {
  left: string;
  top: string;
};

export type Rect = {
  x: number;
  y: number;
  width: number;
  height: number;
};

export type TimelineHeader = {
  date: string,
  dayOfWeek: string,
}